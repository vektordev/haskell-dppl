-- exp of a DECREASING step: exp (0 - x) > 2 iff -x > log 2 iff x < -log 2.
-- The net direction is MonDec, so the transported endpoints swap.
main = draw x = Normal in if exp (0.0 - x) > 2.0 then 1.0 else 0.0
