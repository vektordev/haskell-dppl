-- Interval transport through mult by a NEGATIVE literal (the direction is
-- read off the literal's sign): x * (-2) > 1 iff x < -0.5: Phi(-0.5).
main = draw x = Normal in if (x * (-2.0)) > 1.0 then 1.0 else 0.0
