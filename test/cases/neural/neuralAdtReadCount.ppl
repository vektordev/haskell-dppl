data Attrs = Nil | Obj color::Color
data Color = Red | Blue

neural readAttrs :: (Symbol -> Attrs) of {Nil | Obj {Red | Blue}}

match o = if isNil o then 0 else (if isRed (color o) then 1 else 0)
main s1 s2 = (match (readAttrs s1)) ++ (match (readAttrs s2))
